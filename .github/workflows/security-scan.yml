name: Security Scan

on:
  pull_request:
    branches: [develop, main]

jobs:
  security-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "placeholder security-scan check"
