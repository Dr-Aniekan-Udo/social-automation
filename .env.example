# Copy this file to `.env` and update values for your environment.
# Safe local defaults are provided for development only.

# PostgreSQL connection URL used by backend services.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/social_automation?sslmode=disable
# Redis connection URL used for cache, sessions, and event streaming.
REDIS_URL=redis://localhost:6379/0

# JWT signing secret for short-lived access tokens.
JWT_ACCESS_SECRET=replace-with-local-access-secret
# JWT signing secret for long-lived refresh tokens.
JWT_REFRESH_SECRET=replace-with-local-refresh-secret

# Primary AI provider API key (DeepSeek).
DEEPSEEK_API_KEY=replace-with-deepseek-key
# Secondary AI provider API key (Google Gemini).
GEMINI_API_KEY=replace-with-gemini-key
# Tertiary AI provider API key (OpenAI).
OPENAI_API_KEY=replace-with-openai-key
# Meta Graph API key for Instagram/Facebook platform integrations.
META_GRAPH_API_KEY=replace-with-meta-graph-key
# WhatsApp Business API key for messaging integration.
WHATSAPP_API_KEY=replace-with-whatsapp-key
# Paystack secret key for primary payment processing.
PAYSTACK_SECRET_KEY=replace-with-paystack-key
# Flutterwave secret key for payment fallback support.
FLUTTERWAVE_SECRET_KEY=replace-with-flutterwave-key
